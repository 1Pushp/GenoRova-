# Genorova AI — Railway Deployment Requirements
# Generated April 2026 | Python 3.11
# All versions pinned for reproducible builds
#
# FIXES vs local requirements:
#   torch==2.11.0+cpu  →  torch==2.4.1   (+cpu tag not valid on PyPI)
#   rdkit==2026.3.1    →  rdkit==2023.9.6 (year-based ver not on PyPI)

# ── Web API ────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.6
starlette==0.41.0
pydantic==2.9.2
pydantic-core==2.23.4
python-dotenv==1.0.1
anyio==4.6.0
h11==0.14.0
click==8.1.7
httptools==0.6.4
watchfiles==0.24.0
websockets==13.1
annotated-types==0.7.0
typing-extensions==4.12.2
sniffio==1.3.1
idna==3.10

# ── Chemistry ─────────────────────────────────────────────────────────────
# rdkit available on PyPI since 2022 — use a stable pinned version
rdkit==2023.9.6

# ── Machine Learning ──────────────────────────────────────────────────────
# Plain package names — pip installs CPU builds on Railway (no GPU)
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# ── Data science ──────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
joblib==1.4.2
threadpoolctl==3.5.0

# ── Imaging ───────────────────────────────────────────────────────────────
Pillow==10.4.0

# ── Utilities ─────────────────────────────────────────────────────────────
tqdm==4.66.5
requests==2.32.3
six==1.16.0
python-dateutil==2.9.0.post0
tzdata==2024.2
packaging==24.1
PyYAML==6.0.2
colorama==0.4.6
